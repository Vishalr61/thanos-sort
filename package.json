{
  "name": "thanos-sort",
  "version": "1.0.0",
  "description": "The only sorting algorithm that's perfectly balanced, as all things should be.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Vishalr61/thanos-sort.git"
  }
}
